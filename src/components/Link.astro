---
interface Props {
  text: string;
  url: string;
  variant?: "primary" | "secondary" | "outline";
}

const { text, url, variant } = Astro.props;
---

<a
  class:list={[
    "h-fit w-fit rounded-md bg-accent px-6 py-3 font-semibold text-white",
    { "border border-light-gray bg-transparent": variant === "outline" },
  ]}
  href={url}>{text}</a
>
